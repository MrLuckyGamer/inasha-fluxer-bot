# Copy this file to .env and fill in your values
# Then load it with: node --env-file=.env index.js

# Your Fluxer bot token (from the Fluxer developer portal)
FLUXER_BOT_TOKEN=your_bot_token_here

# Command prefix (default: i>)
prefix=i>
